# Commits that only reformat. `git blame` skips these with:
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Append the SHA of the Stage A tree-wide `ruff format` commit once it exists.
