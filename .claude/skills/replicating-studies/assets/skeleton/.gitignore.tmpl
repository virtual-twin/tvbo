# Run/build outputs — regenerated, not source.
output/
results/
logs/
kit*/
*.tar.gz
__pycache__/
.ipynb_checkpoints/

# Data & upstream material — never vendored; obtain per input/DATA.md.
input/sourcedata/          # third-party raw inputs (connectomes, atlases, …)
original_study/*           # the paper's own material + its published data (workbook, source_data, code)
!original_study/analysis/  # …but keep OUR analysis (targets.md, figures.md, backend-fit.md)

# Generated report artifacts + working notes. report.qmd + references.bib +
# report/_quarto.yml + report/_quarto-internal.yml ARE tracked (the render mechanism, no
# copyrighted content). report_internal.pdf is the local-only A/B build that embeds the
# paper's © originals — covered by report/*.pdf below; only the public report.pdf is
# shareable, and both are git-ignored.
report/*.pdf
report/*.log
report/.quarto/
_dev/
