# <Study> — <topic> (TVBO replication)

**Replication** of <Author> et al., *<title>*, <venue> (<Year>). One declarative recipe
(`<Study>.yaml`) + minimal plotting; a *replication*, not a bit-exact reproduction (see
the report's scorecard). Backend chosen by fit — see `original_study/analysis/backend-fit.md`.

## Layout (FAIR)
```
<Study>/
  <Study>.yaml              THE spec — one declarative recipe at the root (all/selected experiments)
  README.md  environment.yml / requirements.txt
  code/                     ALL code (the spec stays out of code/)
    recipe/                 callables the spec references, reached via `code_source: ./code/recipe`
    prepare_<study>.py  <study>_reference.py (verify)  figures/plot.py
  input/
    DATA.md                 TRACKED — provenance + exact download/regenerate steps
    sourcedata/             third-party raw inputs (connectomes, …) — git-ignored
  output/                   EVERYTHING generated (git-ignored): run outputs, figures/, KPI tables, report.pdf
  report/                   report.qmd + references.bib + writeup   (report.pdf / _dev/ git-ignored)
  original_study/           paper + its published data — git-ignored, obtained per DATA.md
    <paper.pdf>  img/fig*.png  analysis/ (targets, figures, backend-fit)  source_data/  <pristine paper code>
```
## Reproduce
```bash
python code/prepare_<study>.py            # (only if the study has external inputs) raw source -> tvbo-ready Network
tvbo run <Study>.yaml                      # all experiments in dependency order (--experiment for a subset)
python code/figures/plot.py               # all figures + A/B composites -> output/figures/
quarto render report/report.qmd --to pdf  # every number computed, no hardcoding
```
