FROM odoo:19

USER root
RUN export DEBIAN_FRONTEND=noninteractive && \
    apt-get update && \
    apt-get install -y python3-pip && \
    python3 -m pip install --break-system-packages --ignore-installed tvbo tvboptim pydantic pypdf && \
    mkdir -p /usr/local/lib/python3.12/dist-packages/tvbo/.temp && \
    chown -R odoo:odoo /usr/local/lib/python3.12/dist-packages/tvbo/.temp && \
    apt-get clean && rm -rf /var/lib/apt/lists/*
USER odoo
