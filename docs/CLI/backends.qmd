---
title: "Backends (moved)"
description: "This page moved to Run on a backend."
---

The backend reference now lives at **[Run on a backend](../Simulation/Backends.qmd)** —
one page covering every backend, how to invoke it from Python and the CLI, and how the
workflow planner places parameter sweeps.

<!--
This file is retained only so its old URL (/CLI/backends.html) redirects to the canonical
page via the `aliases:` entry on docs/Simulation/Backends.qmd. It is excluded from the
render list in _quarto.yml. Safe to `git rm` once no external links depend on the path.
-->
