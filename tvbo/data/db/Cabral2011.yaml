key: Cabral2011
simulation_experiments:
  - id: 1
    network:
      number_of_nodes: 66
      transforms:
        - name: weight
          equation:
            lhs: C_prime
            rhs: C / (1 / N * Sum(Sum(C[i, j], (j, 1, N)), (i, 1, N)))

    model:
    