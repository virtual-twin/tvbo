---
# Seeded as docs/report_internal.qmd. The INTERNAL A/B entry — a thin wrapper, NOT a second copy
# of the report. It overrides only the output name and title, then includes the real report.
# Because the input filename starts with "report_internal", report.qmd's setup cell sets
# INTERNAL=True and draws the paper's © figures beside each reproduction. `quarto render` builds
# this together with report.qmd (see _quarto.yml `render:`). report_internal.pdf embeds ©-author
# figures and is git-ignored — NEVER commit or distribute it.
title: "TVBO replication of <Author> et al. (<Year>) — internal A/B build (do not distribute)"
format:
  pdf:
    output-file: report_internal.pdf
---

{{< include report.qmd >}}
