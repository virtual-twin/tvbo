# {study}

One paragraph on what this study asks, what it simulates and what it produces. Say what a reader should take from the report.

This is a BIDS study dataset (`DatasetType: "study"`). `{study}.yaml` is the whole specification; everything under `derivatives/`, `docs/figures/` and `.tvbo/` is reproduced by running it, and each result container's `.yaml` sidecar records the run that produced it.

## Layout

<!-- BEGIN STUDY LAYOUT (generated by `tvbo study layout --sync`; do not edit) -->
<!-- END STUDY LAYOUT -->

## Run it

```bash
tvbo validate study .                 # the tree and the filenames conform to the layout
tvbo run {study}.yaml                 # experiments in dependency order, then the figures
tvbo figure render {study}.yaml       # re-render figures only
cd docs && quarto render              # report.pdf
```

Inputs this study does not compute are listed in `sourcedata/README.md` with the steps to obtain them. They are not committed.
