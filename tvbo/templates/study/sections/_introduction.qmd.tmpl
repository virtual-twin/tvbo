# Introduction

What question the studies under this collection answer together, and why any one of them on its own does not. Say what is at stake before saying what was done.

State the claim the document argues. Cite with `@key` against `docs/references.bib`; quote a number as `{{< meta results.<key> >}}` so the manifest is the only place it is written.
