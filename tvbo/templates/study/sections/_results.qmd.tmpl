# Results

One subsection per claim, each opening with the finding rather than the procedure. Every number comes from the manifest as `{{< meta results.<key> >}}`; a value typed in by hand is a value that goes stale silently.

Figures are rendered by the studies that own them and referenced with `@fig-<id>`. A result that came out negative is reported here in the same voice as one that did not.
