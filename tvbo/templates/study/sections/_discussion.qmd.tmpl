# Discussion

What the collection establishes, what it does not, and which of the two the reader is most likely to confuse. Name the limits that a further study would have to remove, not the ones that sound modest.

Close on what the members share: the one specification each was run from, and what that makes checkable that a described method does not.
