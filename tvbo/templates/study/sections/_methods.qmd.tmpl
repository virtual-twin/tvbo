# Methods

Each nested study states its own model, network and protocol in its own report; this section says only what holds across them — the shared parcellation, the common backend, the analyses that span the collection — and points at the members for the rest.

Name each member study once, as its recipe names it, so a reader can go from a sentence here to the study that produced it.
