name: Raw
label: Raw State Recording
description: Records all state variables at every integration step without any transformation.
  Identity passthrough of the full simulation state.
reconcile: by_label
pipeline:
- name: raw
  equation:
    rhs: X
    latex: false
  output: raw
  apply_on_dimension: time
class_reference:
  name: Raw
  module: tvb.simulator.monitors
