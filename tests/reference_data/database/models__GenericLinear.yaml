name: GenericLinear
iri: tvbo:GenericLinear
parameters:
  gamma:
    name: gamma
    definition: 'Slope parameter of the state-variable ''x_GL'', representing the
      inverse of the relaxation time.


      Note: The ‘minus’ sign for the self-connections, scaled by ''gamma_GL'', guarantees
      that the nodes have a stable equilibrium point (ref).'
    value: -10.0
    domain:
      enforce: none
      lo: -100.0
      hi: 0.0
      step: 1.0
      log_scale: false
    description: Slope parameter of the state-variable 'x_GL', representing the inverse
      of the relaxation time
description: The Generic Linear (GL) is a phenomenological, linear one-dimensional  (i.e.,
  one state-variable ('x')) neural mass model (Galán, 2008).
references:
- Galan2008
coupling_inputs:
  c_glob:
    name: c_glob
    dimension: 1
    local: false
  local_coupling:
    name: local_coupling
    dimension: 1
    local: true
state_variables:
  x:
    name: x
    domain:
      enforce: none
      lo: -1.0
      hi: 1.0
      log_scale: false
    description: State-variable of the Generic Linear model
    equation:
      lhs: Derivative(x, t)
      rhs: c_glob + gamma*x + local_coupling*x
      latex: false
    record: true
    variable_of_interest: true
    coupling_variable: true
    equation_type: differential
    equation_order: 1
    initial_value: 0.1
number_of_modes: 1
model_type: generic
autonomous: true
cse: false
compile: false
