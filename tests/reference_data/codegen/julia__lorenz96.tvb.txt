# Auto-generated standalone model file
import numpy as np
import scipy.special
from tvb.basic.neotraits.api import Attr, Final, List, NArray, Range
from tvb.simulator.models.base import Model


class Lorenz96(Model):

    F = NArray(
        label=r":math:`F`",
        default=np.array([0.01]),
        doc="""Constant large-scale forcing.""",
    )
    N = NArray(
        label=r":math:`N`",
        default=np.array([10]),
        doc="""Number of state variables (dimension explicitly expanded below).""",
    )

    _nvar = 10
    state_variables = ["x0", "x1", "x2", "x3", "x4", "x5", "x6", "x7", "x8", "x9"]

    state_variable_range = Final(
        label="State Variable ranges [lo, hi]",
        default={
            "x0": np.array([-1000000000.0, 1000000000.0]),
            "x1": np.array([-1000000000.0, 1000000000.0]),
            "x2": np.array([-1000000000.0, 1000000000.0]),
            "x3": np.array([-1000000000.0, 1000000000.0]),
            "x4": np.array([-1000000000.0, 1000000000.0]),
            "x5": np.array([-1000000000.0, 1000000000.0]),
            "x6": np.array([-1000000000.0, 1000000000.0]),
            "x7": np.array([-1000000000.0, 1000000000.0]),
            "x8": np.array([-1000000000.0, 1000000000.0]),
            "x9": np.array([-1000000000.0, 1000000000.0]),
        },
        doc="""Expected ranges of the state variables for initial condition generation and phase plane setup.""",
    )

    variables_of_interest = List(
        of=str,
        label="Variables watched by Monitors",
        choices=("x0", "x1", "x2", "x3", "x4", "x5", "x6", "x7", "x8", "x9"),
        default=("x0", "x1", "x2", "x3", "x4", "x5", "x6", "x7", "x8", "x9"),
        doc="""Default state variables to be monitored.""",
    )

    parameter_names = List(
        of=str,
        label="List of parameters for this model",
        default=["F", "N"],
    )

    local_parameter_names = []

    state_variable_dfuns = Final(
        label="Drift functions",
        default={
            "x0": "F - x0 + x9*(x1 - x8)",
            "x1": "F - x1 + x0*(x2 - x9)",
            "x2": "F - x2 + x1*(x3 - x0)",
            "x3": "F - x3 + x2*(x4 - x1)",
            "x4": "F - x4 + x3*(x5 - x2)",
            "x5": "F - x5 + x4*(x6 - x3)",
            "x6": "F - x6 + x5*(x7 - x4)",
            "x7": "F - x7 + x6*(x8 - x5)",
            "x8": "F - x8 + x7*(x9 - x6)",
            "x9": "F - x9 + x8*(x0 - x7)",
        },
    )

    cvar = np.array([], dtype=np.int32)

    coupling_terms = Final(label="Coupling terms", default=[])

    _R = None
    _stimulus = 0.0
    use_numba = False

    def dfun(self, state_variables, coupling, local_coupling=0):

        # shape (n_sv, n_modes)
        x0 = state_variables[0, :]
        x1 = state_variables[1, :]
        x2 = state_variables[2, :]
        x3 = state_variables[3, :]
        x4 = state_variables[4, :]
        x5 = state_variables[5, :]
        x6 = state_variables[6, :]
        x7 = state_variables[7, :]
        x8 = state_variables[8, :]
        x9 = state_variables[9, :]

        F = self.F

        # Coupling Terms (from coupling array)

        # Derived Variables

        # Time Derivatives
        # Broadcast every derivative to a common per-node shape so constant
        # (state-independent) derivatives — e.g. ``duA/dt = cA`` — stack into a
        # homogeneous array alongside node-shaped ones, matching TVB's own
        # ``numpy.full_like`` handling of constant drifts.
        _derivs = np.broadcast_arrays(
            np.asarray(F - x0 + x9 * (x1 - x8)),  # x0
            np.asarray(F - x1 + x0 * (x2 - x9)),  # x1
            np.asarray(F - x2 + x1 * (x3 - x0)),  # x2
            np.asarray(F - x3 + x2 * (x4 - x1)),  # x3
            np.asarray(F - x4 + x3 * (x5 - x2)),  # x4
            np.asarray(F - x5 + x4 * (x6 - x3)),  # x5
            np.asarray(F - x6 + x5 * (x7 - x4)),  # x6
            np.asarray(F - x7 + x6 * (x8 - x5)),  # x7
            np.asarray(F - x8 + x7 * (x9 - x6)),  # x8
            np.asarray(F - x9 + x8 * (x0 - x7)),  # x9
        )
        return np.array(_derivs)
