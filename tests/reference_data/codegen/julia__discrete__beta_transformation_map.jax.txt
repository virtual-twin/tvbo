import jax.numpy as jnp
import jax.scipy as jsp


def dfun(current_state, t, cX, _p):
    # Parameters
    beta = _p.beta

    # State variables
    x = current_state[0]

    # Derivatives
    dx_dt = jnp.mod(beta * x, 1)

    derivatives = jnp.array(jnp.broadcast_arrays(dx_dt))

    return derivatives
