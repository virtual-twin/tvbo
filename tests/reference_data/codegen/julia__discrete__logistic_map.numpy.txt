import numpy as np


def dfun(current_state, t, cX, _p):
    # Parameters
    r = _p.r

    # State variables
    x = current_state[0]

    # Derivatives
    dx_dt = r * x * (1 - x)

    derivatives = np.array(np.broadcast_arrays(dx_dt))

    return derivatives
