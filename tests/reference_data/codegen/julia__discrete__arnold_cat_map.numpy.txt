import numpy as np


def dfun(current_state, t, cX, _p):
    # State variables
    x = current_state[0]
    y = current_state[1]

    # Derivatives
    dx_dt = np.mod(y + 2 * x, 1)
    dy_dt = np.mod(x + y, 1)

    derivatives = np.array(np.broadcast_arrays(dx_dt, dy_dt))

    return derivatives
