import jax
import jax.numpy as jnp
import jax.scipy as jsp
from typing import Tuple

from tvboptim.experimental.network_dynamics.core.bunch import Bunch
from tvboptim.experimental.network_dynamics.dynamics.base import AbstractDynamics


class HH_Tissue_Q10(AbstractDynamics):
    """HH_Tissue_Q10 neural mass model."""

    STATE_NAMES = ("v", "m", "h", "n")
    INITIAL_STATE = (-65.0, 0.052932, 0.596121, 0.317677)
    AUXILIARY_NAMES = (
        "I_ext",
        "Q10",
        "alpha_h",
        "alpha_m",
        "alpha_n",
        "beta_h",
        "beta_m",
        "beta_n",
    )

    DEFAULT_PARAMS = Bunch(
        C=10.0,
        E_K=-77.0,
        E_L=-54.3,
        E_Na=50.0,
        I_amp=0.08,
        Q10_early=0.333333,
        Q10_late=0.172414,
        g_K=360.0,
        g_L=3.0,
        g_Na=1200.0,
        switch_time=500.0,
    )

    COUPLING_INPUTS = {}

    def dynamics(
        self,
        t: float,
        state: jnp.ndarray,
        params: Bunch,
        coupling: Bunch,
        external: Bunch,
    ) -> Tuple[jnp.ndarray, jnp.ndarray]:
        C = params.C
        E_K = params.E_K
        E_L = params.E_L
        E_Na = params.E_Na
        I_amp = params.I_amp
        Q10_early = params.Q10_early
        Q10_late = params.Q10_late
        g_K = params.g_K
        g_L = params.g_L
        g_Na = params.g_Na
        switch_time = params.switch_time

        v = state[0]
        m = state[1]
        h = state[2]
        n = state[3]

        I_ext = I_amp
        Q10 = jnp.where(jnp.greater(switch_time, t), Q10_early, Q10_late)
        alpha_h = 0.07 * jnp.exp((-65.0 - v) / 20.0)
        alpha_m = jnp.where(
            jnp.equal(v, -40.0),
            1.0,
            0.1 * (v + 40.0) / (1.0 - jnp.exp((-40.0 - v) / 10.0)),
        )
        alpha_n = jnp.where(
            jnp.equal(v, -55.0),
            0.1,
            0.01 * (v + 55.0) / (1.0 - jnp.exp((-55.0 - v) / 10.0)),
        )
        beta_h = 1.0 / (1.0 + jnp.exp((-35.0 - v) / 10.0))
        beta_m = 4.0 * jnp.exp((-65.0 - v) / 18.0)
        beta_n = 0.125 * jnp.exp((-65.0 - v) / 80.0)

        dv_dt = (
            (-g_Na) * m**3 * h * (v - E_Na)
            - g_K * n**4 * (v - E_K)
            - g_L * (v - E_L)
            + I_ext * 1000
        ) / C
        dm_dt = Q10 * (alpha_m * (1 - m) - beta_m * m)
        dh_dt = Q10 * (alpha_h * (1 - h) - beta_h * h)
        dn_dt = Q10 * (alpha_n * (1 - n) - beta_n * n)

        # Determine per-node shape from the first state variable; broadcast
        # any scalar derivatives or auxiliaries so jnp.stack sees uniform
        # rank-1 arrays.
        _per_node_shape = jnp.atleast_1d(dv_dt).shape

        derivatives = jnp.stack(
            [
                jnp.broadcast_to(jnp.atleast_1d(dv_dt), _per_node_shape),
                jnp.broadcast_to(jnp.atleast_1d(dm_dt), _per_node_shape),
                jnp.broadcast_to(jnp.atleast_1d(dh_dt), _per_node_shape),
                jnp.broadcast_to(jnp.atleast_1d(dn_dt), _per_node_shape),
            ],
            axis=0,
        )

        auxiliaries = jnp.stack(
            [
                jnp.broadcast_to(jnp.atleast_1d(I_ext), _per_node_shape),
                jnp.broadcast_to(jnp.atleast_1d(Q10), _per_node_shape),
                jnp.broadcast_to(jnp.atleast_1d(alpha_h), _per_node_shape),
                jnp.broadcast_to(jnp.atleast_1d(alpha_m), _per_node_shape),
                jnp.broadcast_to(jnp.atleast_1d(alpha_n), _per_node_shape),
                jnp.broadcast_to(jnp.atleast_1d(beta_h), _per_node_shape),
                jnp.broadcast_to(jnp.atleast_1d(beta_m), _per_node_shape),
                jnp.broadcast_to(jnp.atleast_1d(beta_n), _per_node_shape),
            ],
            axis=0,
        )

        return derivatives, auxiliaries
