# Auto-generated standalone model file
import numpy as np
import scipy.special
from tvb.basic.neotraits.api import Attr, Final, List, NArray, Range
from tvb.simulator.models.base import Model


class ReducedSetHindmarshRose(Model):

    a = NArray(
        label=r":math:`a`",
        default=np.array([1.0]),
        domain=Range(lo=0.0, hi=1.0, step=1.0),
    )
    b = NArray(
        label=r":math:`b`",
        default=np.array([3.0]),
        domain=Range(lo=0.0, hi=3.0, step=1.0),
    )
    c = NArray(
        label=r":math:`c`",
        default=np.array([1.0]),
        domain=Range(lo=0.0, hi=1.0, step=1.0),
    )
    d = NArray(
        label=r":math:`d`",
        default=np.array([5.0]),
        domain=Range(lo=2.5, hi=7.5, step=1.0),
    )
    r = NArray(
        label=r":math:`r`",
        default=np.array([0.006]),
        domain=Range(lo=0.0, hi=0.1, step=1.0),
    )
    s = NArray(
        label=r":math:`s`",
        default=np.array([4.0]),
        domain=Range(lo=2.0, hi=6.0, step=1.0),
    )
    xo = NArray(
        label=r":math:`xo`",
        default=np.array([-1.6]),
        domain=Range(lo=-2.4, hi=-0.8, step=1.0),
    )
    K11 = NArray(
        label=r":math:`K11`",
        default=np.array([0.5]),
        domain=Range(lo=0.0, hi=1.0, step=1.0),
    )
    K12 = NArray(
        label=r":math:`K12`",
        default=np.array([0.1]),
        domain=Range(lo=0.0, hi=1.0, step=1.0),
    )
    K21 = NArray(
        label=r":math:`K21`",
        default=np.array([0.15]),
        domain=Range(lo=0.0, hi=1.0, step=1.0),
    )
    mu = NArray(
        label=r":math:`mu`",
        default=np.array([3.3]),
        domain=Range(lo=1.1, hi=3.3, step=1.0),
    )
    sigma = NArray(
        label=r":math:`sigma`",
        default=np.array([0.3]),
        domain=Range(lo=0.0, hi=1.0, step=1.0),
    )
    # Array-valued model constant (e.g. mode-coupling matrix); a fixed class
    # attribute rather than a per-region NArray.
    A_ik = np.array(
        [
            [0.3328165811190223, 0.18525241779532714, 0.3328165811190241],
            [0.5984537933538554, 0.3331114447027479, 0.5984537933538587],
            [0.33281658111902046, 0.18525241779532614, 0.33281658111902235],
        ]
    )
    # Array-valued model constant (e.g. mode-coupling matrix); a fixed class
    # attribute rather than a per-region NArray.
    B_ik = np.array(
        [
            [0.3328165811190223, 0.5984537933538554, 0.33281658111902046],
            [0.18525241779532714, 0.3331114447027479, 0.18525241779532614],
            [0.3328165811190241, 0.5984537933538587, 0.33281658111902235],
        ]
    )
    # Array-valued model constant (e.g. mode-coupling matrix); a fixed class
    # attribute rather than a per-region NArray.
    C_ik = np.array(
        [
            [0.3328165811190223, 0.18525241779532714, 0.3328165811190241],
            [0.5984537933538554, 0.3331114447027479, 0.5984537933538587],
            [0.33281658111902046, 0.18525241779532614, 0.33281658111902235],
        ]
    )
    # Array-valued model constant (e.g. mode-coupling matrix); a fixed class
    # attribute rather than a per-region NArray.
    iV0 = np.array([1.6668762461300366, 0.9278169186250886, 1.666876246130046])
    # Array-valued model constant (e.g. mode-coupling matrix); a fixed class
    # attribute rather than a per-region NArray.
    iU0 = np.array([1.6668762461300366, 0.9278169186250886, 1.666876246130046])
    # Array-valued model constant (e.g. mode-coupling matrix); a fixed class
    # attribute rather than a per-region NArray.
    iV3 = np.array([0.5999245608794813, 1.0777988414803623, 0.5999245608794778])
    # Array-valued model constant (e.g. mode-coupling matrix); a fixed class
    # attribute rather than a per-region NArray.
    iU3 = np.array([0.5999245608794813, 1.0777988414803623, 0.5999245608794778])
    # Array-valued model constant (e.g. mode-coupling matrix); a fixed class
    # attribute rather than a per-region NArray.
    iV4 = np.array([0.35990947874643836, 1.1616503426964113, 0.3599094787464342])
    # Array-valued model constant (e.g. mode-coupling matrix); a fixed class
    # attribute rather than a per-region NArray.
    iU4 = np.array([0.35990947874643836, 1.1616503426964113, 0.3599094787464342])
    # Array-valued model constant (e.g. mode-coupling matrix); a fixed class
    # attribute rather than a per-region NArray.
    iZV = np.array([-3.033148827134955, 1.3877787807814457e-17, 3.033148827134998])
    # Array-valued model constant (e.g. mode-coupling matrix); a fixed class
    # attribute rather than a per-region NArray.
    iZU = np.array([-3.033148827134955, 1.3877787807814457e-17, 3.033148827134998])

    _nvar = 6
    state_variables = ["xi", "eta", "tau", "alpha", "beta", "gamma"]
    number_of_modes = 3

    state_variable_range = Final(
        label="State Variable ranges [lo, hi]",
        default={
            "xi": np.array([-4.0, 4.0]),
            "eta": np.array([-25.0, 20.0]),
            "tau": np.array([2.0, 10.0]),
            "alpha": np.array([-4.0, 4.0]),
            "beta": np.array([-20.0, 20.0]),
            "gamma": np.array([2.0, 10.0]),
        },
        doc="""Expected ranges of the state variables for initial condition generation and phase plane setup.""",
    )

    variables_of_interest = List(
        of=str,
        label="Variables watched by Monitors",
        choices=("xi", "eta", "tau", "alpha", "beta", "gamma"),
        default=("xi", "eta", "tau"),
        doc="""Default state variables to be monitored.""",
    )

    parameter_names = List(
        of=str,
        label="List of parameters for this model",
        default=[
            "a",
            "b",
            "c",
            "d",
            "r",
            "s",
            "xo",
            "K11",
            "K12",
            "K21",
            "mu",
            "sigma",
            "A_ik",
            "B_ik",
            "C_ik",
            "iV0",
            "iU0",
            "iV3",
            "iU3",
            "iV4",
            "iU4",
            "iZV",
            "iZU",
        ],
    )

    local_parameter_names = []

    state_variable_dfuns = Final(
        label="Drift functions",
        default={
            "xi": "IE_i + eta - tau + K11*(-xi + np.dot(xi, A_ik)) + b_i*xi**2 + local_coupling*xi - K12*(-xi + np.dot(alpha, B_ik)) - a_i*xi**3 + np.sum(c_glob0, axis=-1, keepdims=True)",
            "eta": "c_i - eta - d_i*xi**2",
            "tau": "-m_i - r*tau + r*s*xi",
            "alpha": "II_i + beta - gamma + K21*(-alpha + np.dot(xi, C_ik)) + f_i*alpha**2 + local_coupling*xi - e_i*alpha**3 + np.sum(c_glob0, axis=-1, keepdims=True)",
            "beta": "h_i - beta - p_i*alpha**2",
            "gamma": "-n_i - gamma*r + alpha*r*s",
        },
    )

    cvar = np.array([0, 3], dtype=np.int32)

    coupling_terms = Final(label="Coupling terms", default=["c_glob0", "c_glob1"])

    _R = None
    _stimulus = 0.0
    use_numba = False

    def update_derived_parameters(self):

        a = self.a
        b = self.b
        c = self.c
        d = self.d
        r = self.r
        s = self.s
        xo = self.xo
        K11 = self.K11
        K12 = self.K12
        K21 = self.K21
        mu = self.mu
        sigma = self.sigma
        A_ik = self.A_ik
        B_ik = self.B_ik
        C_ik = self.C_ik
        iV0 = self.iV0
        iU0 = self.iU0
        iV3 = self.iV3
        iU3 = self.iU3
        iV4 = self.iV4
        iU4 = self.iU4
        iZV = self.iZV
        iZU = self.iZU

        self.IE_i = IE_i = np.sqrt(sigma) * (iV0 * mu + iZV * sigma)
        self.II_i = II_i = np.sqrt(sigma) * (iU0 * mu + iZU * sigma)
        self.a_i = a_i = a * iV4 / sigma
        self.b_i = b_i = b * iV3 / np.sqrt(sigma)
        self.c_i = c_i = c * iV0 * np.sqrt(sigma)
        self.d_i = d_i = d * iV3 / np.sqrt(sigma)
        self.e_i = e_i = a * iU4 / sigma
        self.f_i = f_i = b * iU3 / np.sqrt(sigma)
        self.h_i = h_i = c * iU0 * np.sqrt(sigma)
        self.m_i = m_i = iV0 * r * s * xo * np.sqrt(sigma)
        self.n_i = n_i = iU0 * r * s * xo * np.sqrt(sigma)
        self.p_i = p_i = d * iU3 / np.sqrt(sigma)

    def dfun(self, state_variables, coupling, local_coupling=0):

        # shape (n_sv, n_modes)
        xi = state_variables[0, :]
        eta = state_variables[1, :]
        tau = state_variables[2, :]
        alpha = state_variables[3, :]
        beta = state_variables[4, :]
        gamma = state_variables[5, :]

        a = self.a
        b = self.b
        c = self.c
        d = self.d
        r = self.r
        s = self.s
        xo = self.xo
        K11 = self.K11
        K12 = self.K12
        K21 = self.K21
        mu = self.mu
        sigma = self.sigma
        A_ik = self.A_ik
        B_ik = self.B_ik
        C_ik = self.C_ik
        iV0 = self.iV0
        iU0 = self.iU0
        iV3 = self.iV3
        iU3 = self.iU3
        iV4 = self.iV4
        iU4 = self.iU4
        iZV = self.iZV
        iZU = self.iZU

        IE_i = self.IE_i
        II_i = self.II_i
        a_i = self.a_i
        b_i = self.b_i
        c_i = self.c_i
        d_i = self.d_i
        e_i = self.e_i
        f_i = self.f_i
        h_i = self.h_i
        m_i = self.m_i
        n_i = self.n_i
        p_i = self.p_i

        # Coupling Terms (from coupling array)
        c_glob0 = coupling[0, :]
        c_glob1 = coupling[1, :]

        # Derived Variables

        # Time Derivatives
        # Broadcast every derivative to a common per-node shape so constant
        # (state-independent) derivatives — e.g. ``duA/dt = cA`` — stack into a
        # homogeneous array alongside node-shaped ones, matching TVB's own
        # ``numpy.full_like`` handling of constant drifts.
        _derivs = np.broadcast_arrays(
            np.asarray(
                IE_i
                + eta
                - tau
                + K11 * (-xi + np.dot(xi, A_ik))
                + b_i * xi**2
                + local_coupling * xi
                - K12 * (-xi + np.dot(alpha, B_ik))
                - a_i * xi**3
                + np.sum(c_glob0, axis=-1, keepdims=True)
            ),  # xi
            np.asarray(c_i - eta - d_i * xi**2),  # eta
            np.asarray(-m_i - r * tau + r * s * xi),  # tau
            np.asarray(
                II_i
                + beta
                - gamma
                + K21 * (-alpha + np.dot(xi, C_ik))
                + f_i * alpha**2
                + local_coupling * xi
                - e_i * alpha**3
                + np.sum(c_glob0, axis=-1, keepdims=True)
            ),  # alpha
            np.asarray(h_i - beta - p_i * alpha**2),  # beta
            np.asarray(-n_i - gamma * r + alpha * r * s),  # gamma
        )
        return np.array(_derivs)
